import { GET_PRODUCTS, GET_PRODUCTS_REQUEST, GET_PRODUCTS_ERROR } from '../constants/product';
import Axios from '../Axios/Axios';

export const searchProducts = (storeName) => dispatch => {
    
  // HTTP request to the backend for products

  } 